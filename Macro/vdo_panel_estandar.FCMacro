# -*- coding: utf-8 -*-
# =====================================================================
# vdo_panel_estandar.FCMacro
# Envoltorio ultraligero para Panel Estándar
# Axioma 3: Capa UI - solo parámetros
# =====================================================================

import sys
import FreeCAD as App

# Bootstrap
_vdo_macro_dir = App.getUserMacroDir(True)
if _vdo_macro_dir not in sys.path:
    sys.path.insert(0, _vdo_macro_dir)

from vdo_panel_core import crear_pieza

# Configuración de variante
configuracion = {
    "tipo": "estandar",
    "nombre": "Panel_Base",
    "cantos": {
        "norte": False,
        "sur": False,
        "este": True,
        "oeste": False,
    },
    "etiqueta": "Panel Estándar"
}

crear_pieza(configuracion)
